# Scope

This directory contains vendored binary artifacts included with the operations package.

# Vendored Artifact Policy

Do not edit binary JARs in place. If an artifact must change, replace it deliberately with a versioned artifact and update any script or documentation that names the old version.

# Core Artifacts

- `HGNCtoOWL-2.0-jar-with-dependencies.jar`: retained HGNC conversion artifact.

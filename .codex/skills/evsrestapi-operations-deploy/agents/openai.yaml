interface:
  display_name: "EVSRESTAPI Operations Deploy"
  short_description: "Build and deploy EVS operations via Jenkins"
  default_prompt: "Use $evsrestapi-operations-deploy to build and deploy the EVSRESTAPI operations release through Jenkins."

policy:
  allow_implicit_invocation: true
